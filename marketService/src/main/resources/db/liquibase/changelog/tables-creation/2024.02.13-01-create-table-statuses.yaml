databaseChangeLog:
  - changeSet:
      id: 2
      author: mikhail_stoma
      preConditions:
        - onFail: MARK_RAN
          not:
            tableExists:
              tableName: statuses
      changes:
        - createTable:
            tableName: statuses
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    nullable: false
              - column:
                  name: name
                  type: varchar(128)
                  constraints:
                    nullable: false
        - addPrimaryKey:
            tableName: statuses
            columnNames: id
            constraintName: pk_status_id
